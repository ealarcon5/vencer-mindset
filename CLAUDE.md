# CLAUDE.md — Vencer Mindset App

## Project Overview

Vencer Mindset App is a new project in the early stages of development. The repository has been initialized but does not yet contain application source code, a build system, or dependencies.

## Repository Structure

```
vencer-mindset/
├── CLAUDE.md          # AI assistant guide (this file)
└── README.md          # Project readme
```

## Current State

- **Status**: Freshly initialized repository — no framework, dependencies, or source code have been set up yet.
- **Branch strategy**: `main` is the default branch on the remote.

## Development Setup

No dependencies or build tooling have been configured yet. When the project is scaffolded, update this section with:

- Language/runtime version requirements
- Package manager and install command (e.g., `npm install`)
- Environment variable setup (`.env` files)
- Database or external service requirements

## Build / Run / Test Commands

_None configured yet._ Update this section as commands become available:

- **Install**: `<TBD>`
- **Dev server**: `<TBD>`
- **Build**: `<TBD>`
- **Lint**: `<TBD>`
- **Test (all)**: `<TBD>`
- **Test (single file)**: `<TBD>`

## Code Conventions

_To be established once the codebase is scaffolded._ Recommended defaults to adopt:

- **Naming**: Use camelCase for variables/functions, PascalCase for components/classes, UPPER_SNAKE_CASE for constants.
- **File naming**: Use kebab-case for file names (e.g., `user-profile.ts`), or match the framework's convention.
- **Formatting**: Adopt Prettier or an equivalent auto-formatter with consistent settings.
- **Linting**: Use ESLint (for JS/TS projects) with a shared config.
- **Imports**: Prefer absolute/alias imports over deep relative paths.
- **Types**: Prefer TypeScript with strict mode enabled.

## Architecture Guidelines

_To be defined._ When architecture decisions are made, document:

- Framework choice and reasoning
- State management approach
- Routing strategy
- API layer / data fetching patterns
- Component organization (feature-based vs. type-based)
- Styling approach (CSS modules, Tailwind, styled-components, etc.)

## Git Workflow

- **Default branch**: `main`
- Write clear, descriptive commit messages focused on *why* rather than *what*.
- Keep commits atomic — one logical change per commit.
- Create feature branches off `main` for new work.

## AI Assistant Notes

- This is a greenfield project. When scaffolding, prefer modern, well-supported tooling.
- Always read existing files before proposing modifications.
- Do not add unnecessary complexity — keep solutions minimal and focused.
- Update this CLAUDE.md file whenever significant project decisions are made (framework choice, new tooling, architectural patterns, etc.).
